#include <bits/stdc++.h>
//如果构造字符串会超时，只要反映射回去找就行了
int main()
{
    std::string s;
    //int n = s.size();
    //位置写的不对，此时还未读入
    long long N;
    std::cin >> s >> N;
    N--;
    long long len = s.size();
}